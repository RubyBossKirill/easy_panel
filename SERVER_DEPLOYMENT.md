# 🚀 Развертывание Easy Panel на сервере

Эта инструкция описывает процесс развертывания Easy Panel на вашем сервере с уже работающими Traefik, N8N и PostgreSQL.

## 📋 Что будет развернуто

Easy Panel будет работать **полностью изолированно** от ваших существующих сервисов:

- ✅ **Отдельная PostgreSQL** для Easy Panel (не затрагивает вашу текущую БД)
- ✅ **Отдельный Adminer** для управления БД Easy Panel
- ✅ **Backend** (Rails API)
- ✅ **Frontend** (React UI)
- ✅ Все через **Traefik** с автоматическим SSL

## 🌐 Поддомены

После развертывания приложение будет доступно по адресам:

- `https://panel.bulatova-psy.ru` - Frontend (React UI)
- `https://api.panel.bulatova-psy.ru` - Backend (Rails API)
- `https://db.panel.bulatova-psy.ru` - Adminer для Easy Panel

## 📝 Шаг 1: Подготовка на локальной машине

### 1.1 Генерация ключей безопасности

На вашей локальной машине выполните:

```bash
# Сгенерировать пароль для БД
openssl rand -base64 32

# Сгенерировать SECRET_KEY_BASE (64 символа)
openssl rand -hex 64

# Сгенерировать ключи шифрования (32 символа каждый)
openssl rand -hex 32  # PRIMARY_KEY
openssl rand -hex 32  # DETERMINISTIC_KEY
openssl rand -hex 32  # KEY_DERIVATION_SALT
```

**Сохраните эти ключи** - они понадобятся на следующем шаге!

## 🔧 Шаг 2: Настройка на сервере

### 2.1 Подключитесь к серверу

Откройте Termius и подключитесь к серверу `194.87.76.75`

### 2.2 Создайте директорию для Easy Panel

```bash
mkdir -p /opt/easy-panel
cd /opt/easy-panel
```

### 2.3 Создайте .env файл

```bash
nano .env.easy-panel
```

Вставьте следующее содержимое (замените значения на сгенерированные ключи):

```env
# Домен (используется из вашего существующего .env)
DOMAIN_NAME=bulatova-psy.ru

# PostgreSQL для Easy Panel (отдельная БД)
EASY_PANEL_DB_NAME=easy_panel_production
EASY_PANEL_DB_USER=easy_panel_user
EASY_PANEL_DB_PASSWORD=ВСТАВЬТЕ_СГЕНЕРИРОВАННЫЙ_ПАРОЛЬ_ДЛЯ_БД

# Rails Backend
SECRET_KEY_BASE=ВСТАВЬТЕ_СГЕНЕРИРОВАННЫЙ_SECRET_KEY_BASE_64_СИМВОЛА

# Ключи шифрования Active Record
ACTIVE_RECORD_ENCRYPTION_PRIMARY_KEY=ВСТАВЬТЕ_СГЕНЕРИРОВАННЫЙ_PRIMARY_KEY
ACTIVE_RECORD_ENCRYPTION_DETERMINISTIC_KEY=ВСТАВЬТЕ_СГЕНЕРИРОВАННЫЙ_DETERMINISTIC_KEY
ACTIVE_RECORD_ENCRYPTION_KEY_DERIVATION_SALT=ВСТАВЬТЕ_СГЕНЕРИРОВАННЫЙ_KEY_DERIVATION_SALT
```

Сохраните файл: `Ctrl+X`, затем `Y`, затем `Enter`

### 2.4 Создайте docker-compose.server.yml

```bash
nano docker-compose.server.yml
```

Скопируйте содержимое из файла `docker-compose.server.yml` из репозитория и вставьте в терминал.

Сохраните файл: `Ctrl+X`, затем `Y`, затем `Enter`

### 2.5 Создайте скрипт развертывания

```bash
nano deploy-easy-panel.sh
```

Скопируйте содержимое из файла `deploy-to-server.sh` из репозитория и вставьте в терминал.

Сохраните файл и сделайте его исполняемым:

```bash
chmod +x deploy-easy-panel.sh
```

## 🚀 Шаг 3: Первое развертывание

### 3.1 Экспортируйте переменные окружения

```bash
export $(cat .env.easy-panel | xargs)
```

### 3.2 Запустите развертывание

```bash
./deploy-easy-panel.sh
```

Скрипт автоматически:
- ✅ Создаст директории
- ✅ Загрузит Docker образы из GitHub Container Registry
- ✅ Запустит контейнеры
- ✅ Выполнит миграции БД
- ✅ Загрузит seed данные (тестовые пользователи)

### 3.3 Проверка развертывания

После завершения скрипта проверьте статус контейнеров:

```bash
docker-compose -f docker-compose.server.yml --env-file .env.easy-panel ps
```

Все контейнеры должны быть в статусе `Up`.

## 🔍 Шаг 4: Проверка работы

### 4.1 Проверьте доступность

Откройте в браузере:

- `https://panel.bulatova-psy.ru` - должна открыться страница входа
- `https://api.panel.bulatova-psy.ru/health` - должен вернуть `{"status":"ok"}`
- `https://db.panel.bulatova-psy.ru` - должен открыться Adminer

### 4.2 Войдите в систему

Используйте тестовые учетные данные:

**Администратор:**
- Email: `admin@test.com`
- Пароль: `password`

**Менеджер:**
- Email: `manager@test.com`
- Пароль: `password`

**Сотрудник:**
- Email: `employee@test.com`
- Пароль: `password`

## 🔄 Автоматическое обновление через GitHub Actions

### 5.1 Настройте GitHub Secrets

Зайдите в ваш репозиторий на GitHub:
`https://github.com/RubyBossKirill/easy_panel/settings/secrets/actions`

Добавьте следующие секреты:

**`SERVER_HOST`**
```
194.87.76.75
```

**`SERVER_USER`**
```
root
```

**`SERVER_PASSWORD`**
```
x?7mfZNum1UFk.
```

**`GITHUB_TOKEN`** (уже должен быть доступен автоматически)

### 5.2 Проверьте workflow

После push в `main` ветку, GitHub Actions автоматически:
- ✅ Соберет Docker образы
- ✅ Загрузит их в GitHub Container Registry
- ✅ Подключится к серверу
- ✅ Обновит контейнеры
- ✅ Выполнит миграции
- ✅ Перезапустит сервисы

## 📊 Полезные команды

### Просмотр логов

```bash
# Все логи
docker-compose -f docker-compose.server.yml --env-file .env.easy-panel logs -f

# Логи конкретного сервиса
docker-compose -f docker-compose.server.yml --env-file .env.easy-panel logs -f easy_panel_backend
docker-compose -f docker-compose.server.yml --env-file .env.easy-panel logs -f easy_panel_frontend
docker-compose -f docker-compose.server.yml --env-file .env.easy-panel logs -f easy_panel_postgres
```

### Перезапуск сервисов

```bash
# Перезапустить все сервисы
docker-compose -f docker-compose.server.yml --env-file .env.easy-panel restart

# Перезапустить конкретный сервис
docker-compose -f docker-compose.server.yml --env-file .env.easy-panel restart easy_panel_backend
```

### Остановка и запуск

```bash
# Остановить все сервисы
docker-compose -f docker-compose.server.yml --env-file .env.easy-panel down

# Запустить все сервисы
docker-compose -f docker-compose.server.yml --env-file .env.easy-panel up -d
```

### Выполнение команд в контейнере

```bash
# Rails console
docker exec -it easy_panel_backend rails console

# Bash в контейнере backend
docker exec -it easy_panel_backend bash

# Выполнить миграции вручную
docker exec easy_panel_backend rails db:migrate

# Загрузить seed данные заново
docker exec easy_panel_backend rails db:seed
```

### Резервное копирование БД

```bash
# Создать бэкап
docker exec easy_panel_postgres pg_dump -U easy_panel_user easy_panel_production > backup_$(date +%Y%m%d_%H%M%S).sql

# Восстановить из бэкапа
docker exec -i easy_panel_postgres psql -U easy_panel_user easy_panel_production < backup_20260204_120000.sql
```

## 🔒 Безопасность

### Текущие меры безопасности:

- ✅ **Пароли хешированы** с помощью BCrypt (cost 12)
- ✅ **JWT токены** с автоматическим обновлением
- ✅ **Refresh токены** хранятся зашифрованными (AES-GCM)
- ✅ **IP адреса** и **device info** зашифрованы в БД
- ✅ **SSL сертификаты** автоматически от Let's Encrypt через Traefik
- ✅ **CORS** настроен только для вашего домена
- ✅ **Security headers** (X-Frame-Options, X-Content-Type-Options, HSTS)
- ✅ **Изолированная сеть** Docker для внутренних коммуникаций

### Рекомендации:

1. **Измените тестовые пароли** после первого входа
2. **Регулярно делайте бэкапы БД**
3. **Не публикуйте .env файлы** в Git
4. **Обновляйте Docker образы** регулярно

## ❓ Решение проблем

### Контейнеры не запускаются

```bash
# Проверить логи
docker-compose -f docker-compose.server.yml --env-file .env.easy-panel logs

# Проверить переменные окружения
cat .env.easy-panel

# Пересоздать контейнеры
docker-compose -f docker-compose.server.yml --env-file .env.easy-panel down
docker-compose -f docker-compose.server.yml --env-file .env.easy-panel up -d --force-recreate
```

### Ошибка 502 Bad Gateway

Подождите 30-60 секунд - контейнеры могут запускаться. Проверьте логи:

```bash
docker-compose -f docker-compose.server.yml --env-file .env.easy-panel logs -f easy_panel_backend
```

### SSL сертификат не выдается

Проверьте, что DNS записи настроены правильно:

```bash
# На вашей локальной машине
nslookup panel.bulatova-psy.ru
nslookup api.panel.bulatova-psy.ru
nslookup db.panel.bulatova-psy.ru
```

Все должны указывать на `194.87.76.75`

### Миграции не выполняются

```bash
# Выполнить вручную
docker exec easy_panel_backend rails db:create
docker exec easy_panel_backend rails db:migrate
docker exec easy_panel_backend rails db:seed
```

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи контейнеров
2. Убедитесь, что все переменные окружения установлены правильно
3. Проверьте, что порты не заняты другими сервисами
4. Убедитесь, что DNS записи настроены

---

**Готово!** 🎉 Теперь Easy Panel работает на вашем сервере!
