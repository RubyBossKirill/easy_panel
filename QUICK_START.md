# 🚀 Быстрый старт - Что делать дальше

## ✅ Что уже готово

- ✅ Код загружен на GitHub: `https://github.com/RubyBossKirill/easy_panel`
- ✅ Docker конфигурация для сервера с Traefik
- ✅ GitHub Actions для автоматического деплоя
- ✅ Скрипты развертывания
- ✅ Подробная документация

## 📋 Следующие шаги

### Шаг 1: Настройте DNS записи (ВАЖНО!)

Зайдите в панель управления вашего домена `bulatova-psy.ru` и добавьте A-записи:

```
panel.bulatova-psy.ru     →  194.87.76.75
api.panel.bulatova-psy.ru →  194.87.76.75
db.panel.bulatova-psy.ru  →  194.87.76.75
```

**Проверка:** После добавления DNS записей (может занять 5-30 минут), проверьте:

```bash
nslookup panel.bulatova-psy.ru
```

Должен вернуть IP: `194.87.76.75`

---

### Шаг 2: Настройте GitHub Secrets

Зайдите: `https://github.com/RubyBossKirill/easy_panel/settings/secrets/actions`

Добавьте секреты:

**SERVER_HOST**
```
194.87.76.75
```

**SERVER_USER**
```
root
```

**SERVER_PASSWORD**
```
x?7mfZNum1UFk.
```

---

### Шаг 3: Сгенерируйте ключи безопасности

На вашей локальной машине выполните:

```bash
# Пароль для БД
openssl rand -base64 32

# SECRET_KEY_BASE
openssl rand -hex 64

# Ключи шифрования (3 команды)
openssl rand -hex 32
openssl rand -hex 32
openssl rand -hex 32
```

**Сохраните результаты** - они понадобятся на следующем шаге!

---

### Шаг 4: Настройте сервер

Откройте Termius, подключитесь к серверу и выполните:

```bash
# 1. Создайте директорию
mkdir -p /opt/easy-panel
cd /opt/easy-panel

# 2. Создайте .env файл
nano .env.easy-panel
```

Вставьте (замените ВСТАВЬТЕ_XXX на сгенерированные ключи из Шага 3):

```env
DOMAIN_NAME=bulatova-psy.ru

EASY_PANEL_DB_NAME=easy_panel_production
EASY_PANEL_DB_USER=easy_panel_user
EASY_PANEL_DB_PASSWORD=ВСТАВЬТЕ_ПАРОЛЬ_ДЛЯ_БД

SECRET_KEY_BASE=ВСТАВЬТЕ_SECRET_KEY_BASE_64_СИМВОЛА

ACTIVE_RECORD_ENCRYPTION_PRIMARY_KEY=ВСТАВЬТЕ_КЛЮЧ_1
ACTIVE_RECORD_ENCRYPTION_DETERMINISTIC_KEY=ВСТАВЬТЕ_КЛЮЧ_2
ACTIVE_RECORD_ENCRYPTION_KEY_DERIVATION_SALT=ВСТАВЬТЕ_КЛЮЧ_3
```

Сохраните: `Ctrl+X` → `Y` → `Enter`

```bash
# 3. Создайте docker-compose.server.yml
nano docker-compose.server.yml
```

Скопируйте содержимое из файла `docker-compose.server.yml` из репозитория.

Сохраните: `Ctrl+X` → `Y` → `Enter`

```bash
# 4. Создайте скрипт развертывания
nano deploy-easy-panel.sh
```

Скопируйте содержимое из файла `deploy-to-server.sh` из репозитория.

Сохраните и сделайте исполняемым:

```bash
chmod +x deploy-easy-panel.sh
```

---

### Шаг 5: Запустите развертывание

```bash
# Экспортируйте переменные
export $(cat .env.easy-panel | xargs)

# Запустите скрипт
./deploy-easy-panel.sh
```

Скрипт автоматически:
- Загрузит Docker образы
- Запустит контейнеры
- Создаст БД и выполнит миграции
- Загрузит тестовых пользователей

---

### Шаг 6: Проверьте работу

Откройте в браузере:

- `https://panel.bulatova-psy.ru` - страница входа
- `https://api.panel.bulatova-psy.ru/health` - должен вернуть `{"status":"ok"}`
- `https://db.panel.bulatova-psy.ru` - Adminer

**Войдите с тестовыми данными:**

```
Email: admin@test.com
Пароль: password
```

---

## 🔄 Автоматическое обновление

После настройки GitHub Secrets (Шаг 2), каждый push в `main` ветку автоматически:
- ✅ Соберет новые Docker образы
- ✅ Загрузит их на сервер
- ✅ Обновит приложение
- ✅ Выполнит миграции

**Без простоя!** (Zero-downtime deployment)

---

## 📚 Документация

- `SERVER_DEPLOYMENT.md` - Подробная инструкция по развертыванию
- `DEPLOYMENT.md` - Общая документация по деплою
- `SECURITY.md` - Документация по безопасности
- `COMMANDS_CHEATSHEET.md` - Полезные команды

---

## ❓ Проблемы?

### Ошибка 502 Bad Gateway
Подождите 30-60 секунд - контейнеры запускаются. Проверьте логи:

```bash
cd /opt/easy-panel
docker-compose -f docker-compose.server.yml --env-file .env.easy-panel logs -f
```

### SSL сертификат не выдается
Убедитесь, что DNS записи настроены (Шаг 1) и подождите 5-10 минут.

### Контейнеры не запускаются
Проверьте переменные окружения:

```bash
cat .env.easy-panel
```

Убедитесь, что все ключи заполнены.

---

## 🎉 Готово!

После выполнения всех шагов у вас будет:
- ✅ Работающее приложение на `https://panel.bulatova-psy.ru`
- ✅ Автоматический деплой при push в GitHub
- ✅ Изолированная инфраструктура (не мешает N8N и другим сервисам)
- ✅ Автоматический SSL
- ✅ Защищенное хранение данных

**Если возникнут вопросы - обращайтесь!** 🚀
