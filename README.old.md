# Easy Panel - Система управления записями к сотрудникам

Полнофункциональная система для управления записями, клиентами и расписанием сотрудников.

## 🚀 Быстрый старт с Docker

### Требования
- Docker
- Docker Compose

### Запуск всего проекта одной командой

```bash
# Запустить все сервисы
docker-compose up --build

# Или в фоновом режиме
docker-compose up -d --build
```

Готово! Проект запущен:
- 🎨 **Frontend**: http://localhost:3000
- 🔧 **Backend API**: http://localhost:5000
- 🗄️ **PostgreSQL**: localhost:5432

### Остановка сервисов

```bash
docker-compose down

# Удалить все данные (включая БД)
docker-compose down -v
```

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Только frontend
docker-compose logs -f frontend
```

## 📦 Структура проекта

```
easy_panel/
├── docker-compose.yml           # Docker Compose конфигурация
├── .env                         # Переменные окружения
│
├── easy-panel/                  # React Frontend
│   ├── Dockerfile.dev          # Docker для разработки
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   ├── utils/
│   │   └── types/
│   └── package.json
│
└── easy-panel-backend/          # Rails API Backend
    ├── Dockerfile.dev          # Docker для разработки
    ├── app/
    │   ├── controllers/
    │   ├── models/
    │   └── services/
    ├── config/
    └── db/
```

## 🔐 Тестовые аккаунты

После запуска доступны:

| Роль | Email | Пароль |
|------|-------|---------|
| Владелец | owner@company.com | 12345678 |
| Администратор | anna@company.com | 12345678 |
| Сотрудник | mike@company.com | 12345678 |

## 🛠 Локальная разработка (без Docker)

### Backend (Rails)

```bash
cd easy-panel-backend

# Установка зависимостей
bundle install

# Настройка БД
rails db:create
rails db:migrate
rails db:seed

# Запуск сервера
rails server -p 5000
```

### Frontend (React)

```bash
cd easy-panel

# Установка зависимостей
npm install

# Запуск dev сервера
npm start
```

## 📚 API Документация

### Аутентификация

**POST** `/api/v1/auth/login` - Вход в систему
```json
{
  "email": "owner@company.com",
  "password": "12345678"
}
```

**POST** `/api/v1/auth/register` - Регистрация
```json
{
  "email": "user@example.com",
  "password": "password123",
  "password_confirmation": "password123",
  "name": "Имя Пользователя"
}
```

**GET** `/api/v1/auth/me` - Текущий пользователь
```
Authorization: Bearer <token>
```

### Ресурсы

- `/api/v1/users` - Пользователи
- `/api/v1/clients` - Клиенты
- `/api/v1/appointments` - Записи
- `/api/v1/time_slots` - Временные слоты
- `/api/v1/payments` - Платежи
- `/api/v1/roles` - Роли и права

## 🔧 Полезные команды Docker

```bash
# Пересобрать контейнеры
docker-compose up --build

# Зайти в контейнер backend
docker-compose exec backend bash

# Зайти в контейнер frontend
docker-compose exec frontend sh

# Выполнить команду Rails
docker-compose exec backend rails console

# Выполнить миграции
docker-compose exec backend rails db:migrate

# Очистить и пересоздать БД
docker-compose exec backend rails db:reset

# Просмотреть статус контейнеров
docker-compose ps
```

## 🎯 Возможности системы

### Для сотрудников:
- ✅ Управление расписанием
- ✅ Просмотр записей клиентов
- ✅ Управление своими клиентами
- ✅ Статистика и отчеты
- ✅ Уведомления

### Для администраторов:
- ✅ Управление пользователями
- ✅ Настройка ролей и прав
- ✅ Просмотр всех клиентов и записей
- ✅ Управление платежами

### Для владельцев:
- ✅ Полный доступ ко всем функциям
- ✅ Настройки аккаунта
- ✅ Настройки платежных систем

## 📄 Лицензия

MIT License
